# 方块金融 更新记录

## V4.5.6（2026-09-10）

- **说明页面：括号内文字换上更具辨识度的颜色。**
  - 新增可复用组件 `api.client.NoteText`：把说明文本中的 **`（…）` 补充说明渲染成暖金色（#FFC66B）**、**`【…】` 条目标题渲染成亮青色（#7FD4FF）**，正文保持原色；提供 `styled(...)`（转成带样式的组件，可折行/可入提示框）、`styled(List)`、`draw(...)` 与 `drawCentered(...)`（直接绘制，支持一行多段括号）四种用法。
  - **公共滚动文本控件 `ScrollableText` 自动套用该样式** → 桌面帮助页与手机应用「使用说明」页（二者共用 HELP_LINES）一次性全部生效，且样式随自动折行保留。
  - 同步应用到其它说明信息：显示器控制台的「…共 N 条规则（滚动查看上限）」与规则操作反馈（如「（机箱将不再自动执行）」）、交易界面的「股票持仓: N（今日锁定 M）」及其悬浮提示。
  - 说明：`【】` 标题一并着色是为了让条目层次更清楚；若只想让 `（）` 着色，去掉 `NoteText.styled` 中的 HEAD 分支即可（一行代码）。

## V4.5.5（2026-09-10）

- **Mod 列表宣传图改用原生清晰度：** `src/main/resources/logo.png` 由 512×512 恢复为**原生 1536×1536**（与手机 App 封面同源图，均不做压缩缩放）。
  - 已核对 Forge 实现：`ModListScreen` 通过 `resourcePack.getRootResource(logoFile)` 读取 **jar 根目录** 的 `logo.png`，路径不变、仅图片更大更清晰。
  - 代价：jar 体积随两张原生图增至约 9.9MB（用户要求保留原生清晰度，故不做压缩）。

## V4.5.4（2026-09-10）

- **封面水印彻底去除 + 手机 App 使用原生清晰度：**
  - **水印去除重做**：上一版是用矩形补丁覆盖，边缘留有残迹且误盖了右侧的美术边框。本版改为**像素级修复**——自动检测水印遮罩（浅灰文字像素）→ 背景按行向干净区域克隆（保留水平色带纹理）→ **边框竖线按列向上取样重建并精确接到水平色带上沿** → 遮罩边缘 4px 羽化。校验：水印区域浅灰像素由 3218 个降为 **0**。
  - **手机 App 封面改为原生 1536×1536 不压缩**（此前被压到 20×20，清晰度不足）；模组封面仍为 512×512（模组列表显示足够，且控制 jar 体积）。
  - 封面/头像等发布素材同步重新导出（CurseForge 封面 1536 原生清晰、头像 1024 由干净原图重新裁切）。

## V4.5.3（2026-09-10）

- **更换封面与手机 App 图标，规避侵权风险（游戏内逻辑零改动）：**
  - **模组封面** `logo.png`：由原像素牛排图案（与 Minecraft 原版物品贴图风格高度相似，存在侵权风险）更换为新的金融主题封面（金币 + 行情面板 + Block Finance 标题），并**去除右下角的 AI 生成水印**；尺寸 1536→**512×512**，体积 841KB→**516KB**。
  - **手机 App 图标** `assets/generated_mod/textures/app/logo.png`：更换为**手绘 20×20 金币图标**（与 MCphone 内置 App 图标同规格 20×20、透明背景；已放大 16 倍自检 20px 下的可读性）。
  - 备注：新封面为 AI 生成图（已去水印、不含第三方版权素材）；AI 生成内容在发布时建议按平台要求标注。

## V4.5.2（2026-09-09）

- **仓库资料完善版（游戏内逻辑零改动）：**
  - 模组元数据：许可证由 "All Rights Reserved" 改为 **MIT**，作者改为 **XinDeFly**，新增 `displayURL` 与 `issueTrackerURL` 指向本仓库，描述文案扩充（现货/期货/股票/手机 App/显示器控制台/自动化机箱）。
  - 新增仓库文件：`README.md`（功能表、依赖、安装、上手、目录结构、许可、致谢）、`LICENSE`（MIT）、`.gitignore`（排除构建产物、IDE、本机脚本与第三方 jar）、`docs/BUILD.md`（构建/离线构建/常见报错）、`docs/GITHUB_SETUP.md`（网页发布步骤）。
  - 新增 wiki 三页草稿：`docs/wiki/Home.md`、`docs/wiki/使用指南.md`、`docs/wiki/常见问题.md`。
  - 新增发布说明：`RELEASE_v4.5.2.md`。
  - 说明：`libs/MCphone-0.10.2.jar` 为第三方前置，**不随仓库分发**（已在 .gitignore 排除并在文档说明获取方式）。

## V4.5.1（2026-09-09）

> 版号修正说明：上一轮误写为 **4.4.11**（末位超过规则上限 10）。按「末位 1~10，超过即进位」的规则，**4.4.10 之后应为 4.5.1**，本条目即为该版本。

- **搜索框可正常输入 E（及 Q/F/数字键等）：** 原版 `AbstractContainerScreen#keyPressed` 在子控件未消费按键时，会用**背包键（默认 E）直接关闭界面**；而 `EditBox#keyPressed` 对字母键返回 false（字母经 `charTyped` 写入），因此在搜索框里打 "e" 会关掉界面。
  - 新增可复用组件 **`api.client.TextInputGuard`**：文本框处于编辑状态时吞掉会误触发的游戏快捷键（背包/关闭 E、丢弃 Q、副手交换 F、玩家列表 Tab、聊天 T、命令 /、快捷栏 1~9），字母与数字仍经 `charTyped` 正常输入（两条链路互不影响），并支持自定义按键映射。
  - 接入四处物品列表界面：现货终端、期货终端、显示器控制台、手机行情页（手机页新增 `isEditingText()` 供界面层判断）。
- **显示器「保存规则」等按钮反馈更详细：**
  - 点击后新增**完整参数反馈行**（显示 6 秒，绿色）：`已保存规则：铁块 卖出 价格 ≥ 120 时每次 64 个 · 触发：价格达到阈值时`；删除/启停同样给出明确反馈，并区分失败原因（未选物品 / 该物品尚无规则）。
  - 规则摘要由「最多 2 条、缩写格式」升级为**最多 3 条、完整字段**：`物品 卖出≥120 ×64 · 达价 · 启用`，超出时显示 `…共 N 条规则`。

## V4.4.10（2026-09-09）

- **物品搜索：新增中文拼音检索，去掉英文检索。**
  - 新增可复用组件 `api.client.PinyinTable`：内置轻量拼音表 `assets/generated_mod/pinyin.txt`（**20,922 条**，覆盖 CJK 基本区 U+4E00–U+9FFF，无声调，约 190KB），首次使用时惰性加载；提供 `toPinyin / toInitials / hasHan / normalize`。
  - `ItemIndex` 新增**统一搜索入口** `search(query, limit)`：中文名包含 **或 拼音全拼包含**（`tiekuai`）**或 拼音首字母包含**（`tk` / `zsj`）；查询串自动小写、去空白；**不再匹配英文物品 ID**（按需求移除）。
  - 四个搜索框（现货终端、期货终端、显示器控制台、手机行情页）全部改用该统一入口（删除各自重复的循环实现），提示文案改为「搜索物品/拼音」，显示器与手机为「拼音/中文…」。
- 难度自评：中等偏易——难点只在"汉字→拼音"数据表（已内置，一次生成、终身复用），检索逻辑本身是纯字符串匹配。

## V4.4.9（2026-09-09）

- **修复"滚轮滚动瞬间物品临时错位"（V4.4.8 重构引入的回归）：**
  - 根因：新的 `ScrollViewport#renderSlide(...)` 用 `视口宽度 / 行距` 反推列数，而视口宽为 `9×18-2 = 160`，整除得 **8 列**（正确是 9）→ 滑动条带读取的索引变成 `行×8 + 列`，物品被整体错位到相邻格；滚轮停下、原版槽位恢复渲染后又"复原"，因此表现为"临时变动"。
  - 修复：**列数由调用方显式传入**（`renderSlide(..., cellsPerRow, ...)`），并在 `MarketMenu` 新增公共常量 `TOWER_VIEW_COLS = 9`，视口宽度、槽位创建、滚动映射、内容行数换算全部改用它，杜绝同类"用宽度反推"的隐患。

## V4.4.8（2026-09-09）

- **可复用性自查与整改（无功能变化）：**
  1. **修掉一个专用服务器隐患**：`ComputerTowerElementLayout.clientContentSize()` 之前直接引用了客户端类 `ComputerLayout` 的常量，而菜单（双端可达）会调用它——专用服务器上可能因客户端类缺失而报错。现新增**双端共用**的纯数值类 `mod3ce985ee.ComputerGeometry`（外框/边框/内容基准尺寸），`ComputerLayout` 只保留同名别名与渲染职责；方法更名为 `contentSize()`，服务端可安全调用。
  2. **滑动条带绘制进公共控件**：原先写在 `MonitorScreen` 里的"裁剪 + 条带位移 + 左右固定竖边"逻辑，重构为 `ScrollViewport#renderSlide(...)`（新增 `CellPainter` 单元格回调、`contains(...)` 命中接口）——任何界面只要有行式视口即可复用同一套滑动/裁剪/边框行为。
  3. **消除散落的魔数**：机箱布局的间距规则提升为公共常量（`TITLE_BAND / CPU_TOP_INSET / DIMM_ABOVE / DIMM_BELOW / DIMM_HEIGHT / DIMM_PANEL_GAP / PCB_BOTTOM_GAP / PANEL_HEIGHT`），DIMM 绘制改用这些常量（槽位、贴图、菜单三处同源）；`MonitorScreen` 的内容尺寸改为引用设备几何常量，不再重复写 520×280。
  - 自查结论：交互/同步/布局类改动均已落在可复用公共件上（`UiDraw`、`ScrollViewport`、`ScaledDeviceScreen`、`ComputerGeometry`、`MarketMenu` 的公开同步 API、`ComputerTowerBlockEntity#revision`），其余为界面私有实现。

## V4.4.7（2026-09-09）

- **机箱存储视口边框：左右侧独立于滚动。** 滑动裁剪区会把格子外框的左右两条竖线一并裁掉，导致滚轮滚动期间"外框短暂消失、停下又出现"。现改为：裁剪只作用于会上下位移的内容（上下沿保持随内容滑动的现有表现），**左右两条竖边在裁剪之外单独补画**，任何滚动状态下都稳定可见。
- **补齐原版快捷存储操作（按住右键拖拽逐个摆放 / 拖拽分堆等）：**
  - **根因**：原版 `mouseDragged` 会在**客户端**先做 `slot.mayPlace(...)` 校验，而客户端用于渲染镜像的占位容器 `canPlaceItem` 返回 false → 拖拽分堆在本地就被拦掉，服务端根本收不到。
  - **修复**：占位容器区分角色——**客户端镜像放行**放置校验（客户端不是权威），**服务端**仍严格按真实容器规则裁决（未选择机箱时拒绝，不会吞物品）；同时保留"可见格数 + 容器真实容量 + 容器自身规则"三重校验。
  - 现在：按住右键拖拽逐个摆放、左键拖拽平均分堆、Shift+左键快速移动、数字键交换、双击收集、Q 丢弃均与原版一致。

## V4.4.6（2026-09-09）

- **修复滚轮滚动时物品图标/格子框「向右下偏移」：** 滑动期间手绘内容把物品画在 `(x+1, y+1)`，而原版槽位画在 `(x, y)`——GUI 尺寸越大偏移越明显（尺寸 4 时相当于 4 个实际像素），滚轮停止、原版槽位恢复渲染后又"回到正常位置"。现改为与原版完全同坐标。
- **滑动裁剪区改用真实屏幕坐标**：内容被整体缩放过，裁剪矩形不能再直接用内容坐标（GUI 尺寸 ≠1 时会裁错位置）。
- **修复"快速操作 + 滚轮滚动有概率导致物品消失"：**
  - `mayPlace` 增加**容器真实容量**校验：此前只校验"可见格数"，当内存条被拔掉、可见格数尚未刷新时仍允许放入，而原版此时已把物品从光标取走 → 物品凭空消失；
  - 服务端每刻把**可见格数与机箱真实容量对齐**（容量变化时同时钳制滚动行并补发同步包）；
  - 滑动期间父类悬停槽位为 null、且点击被屏蔽，快捷操作不会落到映射未同步的格子上。
- **补齐原版存储快捷键体验：** 快捷移动（Shift+左键）现在**优先放入当前可见的 4 行**，放不下才占用需要滚动的行——不再出现"物品一按就不见了（其实进了看不见的行）"；数字键交换、拖拽分堆、双击收集、Q 丢弃等原版操作均走标准流程（槽位的取放/可放置/移除/最大堆叠均已重写，服务端逐项生效）。

## V4.4.5（2026-09-09）

- **修复显示器存储「扩展行放入物品后无图标且取不出来」（真正的根因）：**
  - **根因一（行映射竞态）**：滚动时**客户端立刻**改了槽位→容器索引的映射，而**服务端要等 SCROLL 包到达**（下一刻）才改。在这个时间差内放入物品，客户端以为在"第 2 行"，服务端却按旧映射存到第 1 行——玩家看到的是"物品不显示图标、也取不回来"。
    → 现在**槽位映射只以服务端为准**：客户端滚动只发请求、不本地改映射（滑动期间槽位隐藏且禁止点击），收到同步包回执后才切行（无论是否在滑动中都套用）；滑动结束时若映射仍未同步则补发一次请求（幂等）。
  - **根因二（列表按需扩容）**：机箱存储列表原本按 9 格创建、只在"首次写入更高索引"时才扩容，导致索引 ≥9 在写入前不可读/不可取。→ 改为**固定按最大容量 81 分配**（容量仍随内存条动态变化），扩容行从第一刻起就可读、可放、可取。
  - 图标刷新仍由 V4.4.4 的"内容变更即推送"保障，因此放入任意行都会立即显示。

## V4.4.4（2026-09-09）

- **修复显示器存储「内存条扩展行放入物品后不显示图标」（功能性缺陷）：**
  - **根因**：放置/取出物品走原版点击流程，**不会**触发 TowerActionPacket，而客户端图标依赖同步包；此前同步包只在选择机箱/滚动等操作时发送，因此**放入内存条扩展行（第 2 行起）的物品客户端根本收不到**——格子可见、可交互，却没有图标。
  - **修复**：`ComputerTowerBlockEntity` 增加变更计数（任何 `setChanged` 自增，覆盖放入/取出/自动化交易/元件增删）；`MarketMenu`（mode 5）在服务端每刻的 `broadcastChanges()` 中检测计数变化，**内容一变就主动补发同步包**（含完整存储列表与容量），客户端随即刷新全部行的图标。
  - 客户端校准阶段改为采用**服务端权威滚动行**（非滑动期间），避免容量变化导致行映射与图标错位。
  - 现在放入物品（任意行、任意滚动位置）、自动化交易产出、内存条增删都会在约 1 刻内同步到显示器界面。

## V4.4.3（2026-09-09）

- **收细界面隔断与设备边框（按截图标注）：**
  - **深色隔断带**：标题栏正下方（18px → PCB 直接顶到标题下沿）与 PCB/电源面板之间（深色带由 13px 收为 **6px**，PCB 底边贴近面板）都明显变细；插槽下沿距面板顶部仍保留 25px。
  - **机箱边框减薄**：外框基准 330×323 → **306×291**，边框 20 → **8**（#27313D 屏幕外框到外侧蒙版的距离大幅收窄），内容区相应放大，内容按 290×275 基准等比铺满。
  - **显示器边框减薄**：外框 560×320 → **536×296**，边框 20 → **8**，内容区 520×280 等比铺满。
  - 主板贴图再次按新宽高比重绘：1920×920（输出 **768×367**），与主板显示区（278×133 ≈ 2.09:1）一致，走线不被拉伸。

## V4.4.2（2026-09-09）

- **机箱界面改为「PCB 刚好包住 CPU+DIMM」的紧凑排布（消除主板上部大片空白）：**
  - 布局改为**自上而下**计算：标题栏下方 12px 即 CPU 顶边，CPU 与左右各 4 条 DIMM 紧贴排列，**PCB 底边 = 插槽下沿 + 12px**，插槽下沿距电源面板顶部仍为 **25px**；金属面板高度保持 110px（背包 9×4 + ROG 图案不变）。
  - 机箱外框基准随之收窄：330×390 → **330×323**（内容 290×283），不再有空白区域浪费高度。
  - 主板贴图按新宽高比重绘：1920×1500 → **1920×870**（输出 768×348），**宽高比与主板区域一致（≈2.2:1），PCB 走线/元件不再被横向拉伸**。
  - 由于内容仍走统一缩放，以上排布在任意「界面尺寸」设置下都保持同样比例——PCB 始终不多不少包住 CPU 与内存插槽。

## V4.4.1（2026-09-09）

- **修复缩放下「悬停高亮不在格子上」：** `ScaledDeviceScreen` 在缩放渲染中传给父类的鼠标坐标已是**内容坐标**，但 `isHovering` 又反算了一次（二次换算），导致悬停槽位判定偏移——高亮框落在格子旁边。现渲染期间标记坐标已换算，悬停高亮/提示框精确落在鼠标所指的格子上。
  - 至此缩放链路完整闭环：外框不缩放；内容（背景、贴图、槽位框、DIMM、物品图标、控件）统一按比例缩放；悬停判定、点击、拖拽、滚轮与提示框锚点全部使用同一变换。

## V4.3.10（2026-09-09）

- **修复「视频设置 → 界面尺寸（GUI 尺寸）」下界面与外框不同步（根因修复）：** 此前外框/背景随窗口等比缩放，而容器槽位（原版固定 18 GUI 像素）、物品图标、输入框不跟随缩放，且菜单槽位坐标只在菜单创建时按当时的窗口尺寸算过一次——因此 GUI 尺寸一变化，格子就与外框错位/溢出。
  - 新增**可复用公共基类** `api.client.ScaledDeviceScreen`：内容一律按**基准尺寸**布局，渲染时连同外框整体等比缩放（`pose.scale`），鼠标命中在 `isHovering` 统一反算，提示框仍在缩放外绘制以保持原生字号。
  - `ComputerLayout` 新增内容基准尺寸常量（机箱 **290×350**、显示器 **520×280**）；`ComputerTowerElementLayout.clientContentSize()` 改为返回**固定基准尺寸**（与窗口/缩放无关）。
  - `TowerStorageScreen`、`MonitorScreen` 改为继承该基类：外壳按真实坐标绘制（不缩放），主板/金属面板贴图、槽位框、DIMM 插槽、物品图标、输入框与滚动视口全部进入同一缩放变换；鼠标点击/拖拽/滚轮坐标同步反算，格子与画面永远对齐。
  - 菜单槽位坐标（机箱元件 8 槽 + 背包 9×4）现在恒等于基准坐标，界面重开或缩放变化后不再错位。

## V4.3.9（2026-09-09）

- **去掉显卡图标：** 主板贴图重绘为**纯 PCB**（走线/过孔/贴片元件/丝印铺满整板），不再绘制显卡（含挡板），腾出整个主板区域。
- **整体缩小机箱 UI：** 机箱外框基准尺寸 390×460 → **330×390**（约缩小 15%），元件界面/开机动画/机箱元件菜单槽位坐标随 `ComputerLayout` 自动等比同步（菜单槽位与渲染始终对齐）。
- **内存插槽与电源间距：** 布局公式调整——插槽底部（槽体高 96）与金属面板顶部保持**恰好 25px** 间隔（`ramY = panelTop - 75`），CPU 相应下移与插槽下部对齐；画面更紧凑。

## V4.3.8（2026-09-09）

- **显卡 LOGO 位置与端部构图纠正：** 参考图为**显卡右前方俯视**拍摄——图左是卡的**右端**（近端），白色 NVIDIA 标志位于**卡右侧**（距右端约 1/4~1/3 处，紧邻金条下沿）。此前误将其画在左端一侧。现修正：
  - NVIDIA 白色漩涡 +「NVIDIA」字样移到**面板右侧**（距右端约 1/4，靠近金色顶条）；
  - **右端**改为**风扇栅格端**（白色小标签 + 细格网方块 + 两个接口小方块 + 横向板条栅格）；
  - **左端**改为**标签端盖**（白色条形码标签 + 微型字 + 小标签 + 螺丝）；
  - 其余（蓝灰面板、金色滚花顶条、上下沿螺丝、底边金线、无挡板）保持不变。

## V4.3.7（2026-09-09）

- **显卡按图示彻底重画（对照 V4.3.6 的差异全部修正）：**
  - **移除**卡体左端的银灰色金属挡板（4 黑孔大灰块）——参考图的 LOGO 侧没有挡板，左端是暗色端盖；
  - 正面面板改为**中等钢蓝色光泽板**（上亮 #4A6C8E、下暗 #26384E，顶部反光带，去掉过重的黑色下压带）；
  - **金色滚花顶条加宽加亮**（占比由 7% 提至 15%，香槟金 + 细密竖向凹凸，上下深灰导轨）；
  - 左端端盖重组：白色小标签 + 细格网方块 + 两个接口小方块 + 横向板条风扇格栅（贯穿左端）；
  - 右端端盖条形码白色标签与微型字、小型标签、端盖螺丝；
  - 面板上沿 6 / 下沿 5 枚椭圆螺丝（参考图分布）；
  - 底边细金色金属边线；
  - NVIDIA 漩涡 + 字样保持白色（左上角，紧邻金条）。
  - 仍按 1080P（1920×1080）绘制后等比缩放合成（视觉自检：整体特写与参考图结构逐项核对通过）。

## V4.3.6（2026-09-09）

- **显卡按识别报告重绘（LOGO 侧）：** 按要求先对开箱实拍图做长文字识别报告（`gpu_logo_side_report.md`），再严格按报告在 **1920×1080（1080P）画布**上绘制显卡，最后**等比缩放**合成到主板贴图显卡区域（768×600 内 568×119），细节保真度更高：
  - 蓝灰色光泽面板（上亮下暗 + 顶部反光带）+ 深枪灰框架；
  - 顶边金色滚花条（细密竖向凹凸，上下深灰导轨）；
  - 左上角**白色** NVIDIA 漩涡标志 + 白色「NVIDIA」字样；
  - 面板上沿 5 / 下沿 4 枚小椭圆螺丝；
  - 左端：白色小标签 + 网格小方块 + 两个接口小方块 + 横向细条栅格；
  - 右端：深色端盖 + 白色条形码标签（竖线 + 微型字）+ 小型白标签 + 螺丝；
  - 底边细金色金属边线；
  - 独立银色金属挡板保持贴合卡体左缘（贴合 6px，绘制在卡体之上）。
  - 不绘制该视角不可见的风扇面 / PCIe 金手指 / 12VHPWR 供电口。

## V4.3.5（2026-09-09）

- **显卡 NVIDIA 标志改回白色：** 按开箱实拍图，NVIDIA 漩涡与「NVIDIA」字样均为**白色**（此前 V4.3.4 按我方误述的 #144421 深绿绘制，已更正）；显卡其余部分（蓝灰面板、金色滚花顶条、螺丝、左端栅格、右端端盖）保持按图示。
  - 需求更正说明：V4.3.4 的「深绿 NVIDIA」为需求表述有误，以本版为准。

## V4.3.4（2026-09-09）

- **显示器机箱存储图标确定性修复：** `TowerSyncPacket` 现在直接携带每个机箱的存储区物品（绝对索引 0..容量-1），客户端收到同步包后写入菜单占位容器——槽位图标不再依赖容器槽位同步包的时序/状态，滚动、切换机箱、物品变化后图标必然刷新。
- **滚动动画改为平滑上下滑动：** `ScrollViewport` 重写为滑动 API（`slideProgress/slideFromRow/slideTargetRow/onScrollRequest`，新增 `DEFAULT_SLIDE_MS`，移除淡入淡出遮罩 `renderFade`）；滚动时旧内容整体滑出、新内容从边缘滑入（180ms）。
  - `MonitorScreen` 改为逐槽位绘制滑动内容条带（视口内 scissor 裁剪，槽位描边 + 物品图标 + 数量角标同步滑动），滑动期间隐藏父类槽位渲染与命中（防错位交互），空闲时恢复。
  - 滚动请求即时刻本地更新映射并向服务端发送 SCROLL（不再等动画结束才发包），服务端回发同步包校准行号。
- **CPU 贴图：** 蚀刻文字统一为 **#4A505A**，去掉此前「AMD / THREADRIPPER / 9995WX」的浅色微浮雕高光（不再有立体浮凸，全部为单层深色蚀刻）。
- **主板贴图：** 显卡按 RTX PRO 6000 开箱实拍图重绘为 **LOGO 侧**——蓝灰色光泽面板 + 枪灰框架、顶边金色滚花散热条（竖纹）、左上白色漩涡 + 深绿（#144421）NVIDIA 字样、面板上下沿小椭圆螺丝、左端风扇栅格（横板条 + 小网格/接口方块 + 白色小标签）、右端深色端盖（白色条形码标签 + 型号细字 + 螺丝）、底边金色细条；去除该视角不可见的 12VHPWR 供电口；金属挡板保持独立于卡体左侧。

## V4.3.3（2026-09-09）

- **图像自检迭代（视觉审查后修正）：**
  - 电源面板 ROG 败家之眼：初版在银色拉丝底上对比度偏低——改为饱和青蓝色填充（`#E2F4FF`）+ 青色边缘（`#82C8F5`）+ 右下柔和深色投影，镂空背光感与分离度同步提升。
  - CPU 贴图：「AMD」与「Ryzen」字距过近——拉开间距并为主标题行（AMD/THREADRIPPER/9995WX）加上下偏移的浅色微浮雕高光。
  - 主板贴图：审查确认单张金色显卡、贴合挡板、绿色 NVIDIA、均匀元件布局无误。

## V4.3.2（2026-09-09）

- **修复显示器存储图标不显示（根因）：** 服务端 `handleAction` 在开头要求 `packet.pos` 处必须是机箱，而视口滚动包（SCROLL）的 pos 字段为 0（(0,0,0) 不是机箱），导致服务端**永远收不到滚动**——客户端本地滚动显示正常，但点击存取按服务端（未滚动）的映射落点，物品存入看不到的索引、图标不显示。现已把 SCROLL 处理提升到机箱校验之前（客户端滚动 → 服务端同步行号 → 回发同步包校准）。
- **主板贴图：** 显卡左侧金属挡板改为**贴合卡体左缘绘制**（右缘与金色侧身重叠 6px，不再分离悬空）。

## V4.3.1（2026-09-09）

- **ROG 败家之眼按官方设计图重绘：** 直接以官方设计图（Fearless Eye 蓝图，比例 1.9X×X）为数据源——扫描红色主体 → 腐蚀去除非主体部分（注释文字/指示线）→ 提取精确轮廓（宽高比 1.88 ≈ 官方 1.9），再以亮青白色等比绘制到电源面板右侧可见区域（600×318 @ 居中）；颜色按需求不做强制（仍为镂空背光风格）。

## V4.2.10（2026-09-09）

- **显示器滚动动画改为逐行独立淡入淡出：** `ScrollViewport` 新增行间距与错峰时长，滚动时 4 行按 62ms 错峰逐行淡出、切换后逐行淡入，不再整块淡出淡入。
- **主板贴图：** 只保留**一张** RTX PRO 6000——宽度 = 原两张之和（1420），高度 = 原 0.9 倍（297），整体**略微下移**（与内存插槽拉开距离）；**金属挡板独立于显卡**，绘制在金色侧身左侧（不再覆盖金色侧板）；「NVIDIA」字样改为**更绿（#60BE00）且字号加大**（22 → 52）。
- 公共控件 `ScrollViewport` 新增构造重载 `(x, y, w, h, visibleRows, rowPitch, fadeMs)`，逐行遮罩高度可与槽位行距对齐（其它界面同样可复用）。

## V4.2.9（2026-09-09）

- **控件与接口全面可复用化（架构整理）：**
  - 新增公共控件 `api.client.UiDraw`：统一配色常量（面板/描边/悬停/选中/文字/金色）、1px 描边底框与按钮、槽位边框、**背包/快捷栏分隔线**、命中测试与颜色插值——任意界面（含其它模组）可直接调用。
  - 新增公共控件 `api.client.ScrollViewport`：通用「行滚动视口」（固定 N 行 + 右侧滑条 + 滚轮 + 拖拽 + 淡出→切换→淡入动画 + 滚动回调），与具体容器无关，任何列表/槽位区域均可复用。
  - `MonitorScreen` 的滑条/淡入淡出/滚动状态机、按钮绘制、分隔线、颜色插值全部改为复用上述公共控件（删除约 120 行重复实现）；`TowerStorageScreen` 的槽位描边与分隔线同样改为复用。
  - 机箱存储视口几何常量提升为 `MarketMenu.TOWER_VIEW_*` 公共常量（界面渲染与命中测试共用同一来源，不再各自硬编码）。

## V4.2.8（2026-09-09）

- **显示器机箱存储改为 4 行滚动视口：** 无论安装多少内存条，机箱存储区最多显示 4 排（36 格）；超出部分通过**右侧滑条**（可点击/拖拽）或**鼠标滚轮**滚动查看。
- **滚动淡入淡出动画：** 滚动时先淡出当前 4 行内容，切换后淡入新内容（0.25 秒），格子进入/离开视野时可见明显的渐现与渐隐。
- **服务端同步滚动行：** 新增 SCROLL 协议（value = 起始行），保证服务端槽位映射与客户端一致（拾取/放置不会错位）。
- 槽位映射改为「视口行 → 容器索引」动态映射（MC 槽位坐标不可移动，故采用容器索引映射实现滚动）。

## V4.2.7（2026-09-09）

- **显卡贴图配色修正：** RTX PRO 6000 侧面改为**土豪金（香槟金）金属侧板**——金渐变底 + 顶部高光 + 细拉丝纹理 + 深色蚀刻「RTX PRO 6000」与「NVIDIA」字样，配银色 I/O 挡板、黑色 12VHPWR 供电口；此前误按风扇面（黑色钢琴烤漆）配色绘制，与实际侧板金色不符。

## V4.2.6（2026-09-09）

- **内存插槽改为服务器级 8 槽：** CPU 左右各 4 条 DIMM 插槽（左右对称），机箱存储上限由 45 格提升至 **81 格**（9 + 8×9）；显示器控制台机箱存储区扩展到 9 行，背包/快捷栏下移。
- **CPU：** 横向加宽（宽高比约 1.4:1）并**顶部居中**，CPU 贴图重新绘制为宽版（1920×1380 绘制 → 1280×920）。
- **主板贴图：** 显卡改为**两张并排横向放置**、**去掉风扇贴图**；PCB 上**不再绘制 PCIe 插槽**；元件仍为均匀网格分布。
- **电源面板贴图：** 顶部丝印右移（不再被左上角螺丝遮挡）；去掉「ROG」文字，仅保留**镂空的败家之眼（Fearless Eye）图标**——多层发光光晕、白色描边眼形、上方斜切眉线、大号发光斜切瞳孔。
- 帮助页同步更新（8 个内存插槽、最多 81 格）。

## V4.2.5（2026-09-09）

- **机箱元件界面：** 去掉 DIMM 插槽下方的绿色走线束（不再遮挡显卡）；CPU 贴图与 4 条 DIMM 插槽整体上移至主板顶部区域。
- **主板贴图：** 显卡水平居中（左右留白对称）；移除显卡底部金手指（插入后本应被插槽完全遮挡）；电子元件改为**均匀网格分布**（电阻/电容/芯片/电解电容/电感按格子铺开，避开显卡与插槽区域），过孔与焊盘同样均匀化。
- **电源面板贴图：** 背包与快捷栏移至**左侧边框旁**，右侧腾出空间绘制**大尺寸 ROG 败家之眼**专属 Logo（发光外圈光晕、金色描边眼形、深色眼白、大号斜切发光瞳孔与高光、金色 ROG 字样 + REPUBLIC OF GAMERS），保留铝合金拉丝底、顶部丝印与四角螺丝。
- **显示器控制台：** 选中机箱方位按钮的呼吸闪烁**去掉上方箭头**，仅保留按钮本身的脉动高亮。

## V4.2.4（2026-09-09）

- **CPU 贴图修正：** 顶盖小字规格移到型号下方左对齐、字号缩小，全部限制在金属顶盖范围内（不再与顶盖边框重叠）。
- **主板贴图修正：** 取消第二条 PCIe 插槽与插槽说明小字；显卡改为**完全占满第一条 PCIe 插槽**（金手指贯穿整条插槽）；显卡整体下移，与 CPU 外框、DIMM 插槽彻底分离；插槽周边补齐贴片电阻/电容、芯片、电解电容与主板安装孔。
- **电源面板贴图 1:1 复刻（ROG 雷神三代带 LOGO 一侧）：** 铝合金拉丝外壳 + 镂空发光 ROG 之眼与 ROG 字样、OLED 功率屏凹槽（含发光环与功率读数）、透明塑料角饰（THOR 刻字）、顶部发光条、型号丝印（ROG THOR 1200W PLATINUM III / 80 PLUS PLATINUM · AURA SYNC）、四角螺丝；面板加高到 110px 并在上方留出 32px 图案展示区，背包网格下移避免完全遮挡。
- **方位按钮呼吸色调整：** 深色更深（#072B42）、浅色降低亮度（#4FA6C8），外框与箭头同步减弱，避免刺眼。

## V4.2.3（2026-09-09）

- **显示器控制台：** 选中机箱方位按钮的呼吸闪烁效果增强（底色在深青蓝 ↔ 近白之间大幅波动、外框与指示箭头同步脉动、周期缩短至 0.3 秒），醒目度明显提升。
- **显示器控制台：** 修复底部数值输入框与旁边文字重叠——「阈值$」改为「阈值」，输入框左移起点调整至 x=404（宽度 110），与文字完全分离。

## V4.2.2（2026-09-09）

- **背包/快捷栏区分：** 所有含存储或玩家背包交互的界面（手机现货页、机箱元件界面、显示器控制台）中，快捷栏行与背包 3 行之间增加 4px 间隔，与玩家正常打开背包一致；机箱元件界面与显示器控制台另加一条分隔线。
- **CPU 贴图化：** 以 AMD 线程撕裂者 PRO 9995WX 为原型，1920×1080 画布绘制后缩小为 1024×1024 贴图——深绿基板 + 边缘贴片元件、银白拉丝金属顶盖、激光蚀刻文字（AMD Ryzen / THREADRIPPER PRO / 9995WX）与小字规格（96 核 192 线程、384MB L3、3.7/5.4GHz、350W TDP、sTR5、8 通道 DDR5-6400、128 条 PCIe 5.0）及 AMD 箭头标志；CPU 区域改为贴图渲染（移除原「CPU」大字）。
- **主板贴图重绘：** 保留原走线/过孔/贴片电阻电容/电解电容/电感/芯片/焊盘/丝印等内容并调整布局，新增 **2 条 PCIe 5.0 x16 插槽**（黑色槽体 + 金手指 + 尾部卡扣）与**插入上槽的显卡侧视图**（以 NVIDIA RTX PRO 6000 为原型：黑色钢琴烤漆外壳、散热鳍片、双风扇外壳弧、PCIe 金手指、I/O 挡板 4 个 DP 口、12VHPWR 供电口、RTX PRO 6000 与 NVIDIA 丝印）。

## V4.2.1（2026-09-09）

- **修复显示器机箱方位按钮高亮失效（根因）：** 服务端处理 TowerActionPacket 时用**机箱**位置作为 `sendSync` 基准去扫描相邻机箱，返回的机箱列表几乎为空，客户端收到后把已选机箱重置为未选，因此点击后高亮立刻消失。现改为按玩家当前打开的控制台菜单（显示器）位置重新扫描，选择状态不再丢失；同时改为按机箱位置恢复选择（不受列表顺序/瞬时同步影响）。
- **方位按钮反馈强化：** 选中机箱的方位按钮采用呼吸式高亮（亮青蓝底 + 亮边渐变）、上方增加指示箭头；「选中(X)」行同步显示当前机箱方位。
- **修复两处贴图被裁切（主板图/金属面板）：** `GuiGraphics.blit` 的 9 参重载会把宽高同时作为采样区域，导致只绘制贴图左上角一小块；改用 11 参重载指定完整采样区域——主板贴图完整铺满、ROG 风格金属面板完整拉伸至面板区域。
- **CPU 尺寸缩小：** 由内容区宽 34%/主板高 72% 调整为 24%/52%（并降低最小值），为走线与主板元件留出更多空间；DIMM 插槽保持上版不变。

## V4.1.10（2026-09-09）

- **机箱元件界面 DIMM 区调整：** 4 条内存插槽改为**紧凑相邻排布**（间距 2px，与真实主板 DIMM 插槽一致），槽体宽度收窄为 **10px**；移除原先各插槽侧面的金色连线，改为**真实主板风格的 PCB 走线束**——从 CPU 右侧引出 4 条并行绿色细走线至各插槽底部并带过孔（金色仅保留为槽内触点）。

## V4.1.9（2026-09-09）

- **机箱元件界面底部金属面板贴图化：** 参考 ROG 雷神 III 电源带 LOGO 一侧（银黑配色、拉丝铝外壳、屏幕安装槽黑金 ROG 字样），以 1920×1080 绘制后拉伸铺满面板区域：拉丝铝渐变底纹 + 对角光泽带、黑色凹槽带、金色 ROG 之眼（几何眼形 + 斜切瞳孔）、金色「ROG」与银白「THOR III」字样、左侧发光角灯、四角十字螺丝、上下倒角高光。

## V4.1.8（2026-09-09）

- **机箱元件界面：** DIMM 内存槽改为更细长的黑色槽体（12×96），金色针脚改到槽内**两侧**竖直排布（符合真实内存插槽触点位置），移除槽位白色外框，让黑色槽体成为视觉主体。
- **显示器控制台：** 选中机箱对应的方位按钮高亮进一步强化——外发光描边 + 亮青蓝底 + 白色文字，鼠标悬停时同样保持高亮，确保一眼可辨当前显示的机箱。

## V4.1.7（2026-09-09）

- 显示器控制台的机箱方位九宫格中，**当前正在显示的机箱所对应的方位按钮改为高亮显示**（亮青蓝底 + 高亮描边 + 白色文字），与其他方向按钮形成明显对比，便于确认当前查看的是哪台机箱。

## V4.1.6（2026-09-09）

- **机箱元件界面：** 底部提示文字移至顶部标题栏右侧（不再与绿色电路板重叠）；内存插槽的黑色槽体改为更细长的竖向 DIMM 造型（14×62，底部金脚）。
- **显示器控制台：** 机箱选择改为《通用机械》式九宫格方向按钮（上/左/前/右/后/下，按该模组六面配置的读取顺序排列），按钮尺寸缩小（26×13）并长期显示——无机箱的方向显示为暗淡空位，随时可点击切换机箱查看各自存储空间。

## V4.1.5（2026-09-09）

- **修复内存条扩容不生效：** 显示器控制台的槽位绑定原先只在服务端发生，客户端仍按 9 格判断可见行数，导致插入内存条后格子数不增加。现通过同步包下发每个机箱的实际容量（9+9×内存条数），客户端据此显示对应行数（选择机箱时立即刷新，服务端同步后再次校准）。
- **主板贴图化：** 机箱元件界面的主板改为高清贴图绘制——以 1920×1500 画布绘制真实 PCB 细节（走线、过孔、贴片电阻/电容、电解电容、电感线圈、芯片 IC、金色焊盘、白色丝印编号），等比缩小为 768×600 贴图后按比例铺满机箱边框内的主板区域；CPU 金属盖与「CPU」大字、黑色内存插槽、金手指色连线仍按布局精确绘制在贴图之上。

## V4.1.4（2026-09-09）

- **机箱开机动画：** 去除左侧蓝色灯条与底部白色电源按钮装饰（机箱外壳全部界面不再绘制装饰，显示器保持原有指示灯）。
- **机箱元件界面重构（自适应主板式）：** 内部 UI 铺满机箱屏幕（随 GUI 缩放自适应，槽位坐标与渲染按内容尺寸等比计算）：
  - 中线以上为大片主板区域（占机箱主要空间）：深色 PCB 底 + CPU 插座（银色金属散热盖、**清晰放大「CPU」大字**、四角螺丝），CPU 右侧 4 个**黑色内存插槽背景**（与真实 DIMM 槽类似）+ 槽底金脚，每个插槽与 CPU 之间以**金手指同色黄色细线**连接；
  - 移除电容、芯片组、PCIe、电池、网格等多余零件；
  - 中线以下为光泽金属灰白面板并**下移**，显示玩家背包/物品栏 9×4。

## V4.1.3（2026-09-09）

- **显示器存储显示修复：** 未选择机箱时控制台仅显示基础 9 格（一排），不再画出 45 格空网格；选择机箱后按其实际容量显示（未装内存条 9 格，逐条扩展）。
- **机箱元件界面重构（主板式）：** 去除左侧蓝色灯条与底部白色电源条装饰；
  - 中线以上为高分辨率电脑主板图：深色 PCB 走线网格、CPU 插座（银色金属散热盖 + 「CPU」大字 + 四角螺丝）、CPU 右侧 4 个竖直内存插槽（金色触脚）显示为**无背景可交互内存条空位**、电容、芯片组、PCIe、主板电池；
  - 中线以下为光泽金属灰白渐变面板，显示玩家背包/物品栏 36 格；
  - 「内存条槽位（每个 +9 格）」提示置于插槽上方。

## V4.1.2（2026-09-09）

- **存储显示修复：** 显示器控制台中列仅绘制当前容量内的槽位（未放置内存条时只显示基础 9 格一排，不再显示满容量空网格；隐藏行不可点击/不能放入物品）。
- **交互调整：** 恢复机箱独立操作界面——点击机箱（开机动画后）直接打开「存储元件」界面，可手动向机箱放入内存条扩容；显示器控制台移除「元件槽」按钮（显示器界面不再包含内存条相关设置），机箱货物存储仍通过显示器查看。
- **贴图重制：** 内存条改为原生 256×256 高清绘制（渐变电路板/金属芯片/标签贴纸/走线过孔/金手指渐变/螺丝孔/防呆缺口，抗锯齿圆角），不再采用 16×16 网格放大。

## V4.1.1（2026-09-09）

- 「内存条」贴图重制为 256×256 高清：细化电路板明暗与走线、双 IC 芯片（含高光/暗边/引脚）、白色标签贴纸、过孔、防呆缺口与金手指引脚明暗层次。
- 模组版号升至 4.1.1。

## V3.10.1（2026-09-09）

- **新物品「内存条」**（mod_3ce985ee:memory_stick）：机箱存储元件道具，放入机箱左侧存储元件槽后每个增加 9 格（一排）存储容量；显示在前置模组（MCphone）主物品栏。
- **内存条合成配方**（3×3）：玻璃/金块/玻璃 · 红石中继器/红石块/红石比较器 · 铜块/铜块/铜块。
- **村民交易**：职业为图书管理员的村民（1 级）以 3 个金锭兑换 1 条内存条（每次 12 次交易、5 经验）。
- 帮助页机箱条目补充内存条获取方式。

## V3.9.10（2026-09-09）

- **存储机制重构（机箱）：** 机箱存储改为动态容量：基础 9 格（一排），每个存储元件 +9 格（上限 45 格）；机箱左侧新增 4 个竖直并排的存储元件槽（仅接受存储元件道具，tag `generated_mod:storage_elements` 判定，道具将在后续加入）。
- **交互调整：** 点击机箱不再打开存储界面（提示改为「请通过显示器控制台操作」）；漏斗/管道等仍可向机箱存储直接传输物品。
- **控制台调整：** 显示器控制台标题行新增「元件槽」按钮（打开所选机箱的存储元件界面）；中列机箱存储按当前容量显示 9~45 格（未安装元件时仅一排），背包格下移对齐。
- **容量保护：** 移除存储元件时若有物品超出缩小后的容量，将被拦截并提示（物品不丢失）；旧版机箱的存量物品自动保留，超出基础容量的部分安装元件后恢复可见。
- 帮助页机箱/显示器条目同步更新。

## V3.9.9（2026-09-09）

- 显示器控制台机箱位置显示取消歧义坐标（如 (-577,6)），改为**中文单字**表示相对位置：上/下/左/右/前/后（以站在显示器正面的观察者视角，前=屏幕朝向侧；机箱选择按钮与「机箱存储:」提示同步生效）。

## V3.9.8（2026-09-09）

- 电脑机箱与显示器新增水平朝向属性（facing）：**放置时正面（机箱面板/显示器屏幕）自动朝向玩家**，blockstate 提供四方向旋转变体；此前方块固定朝北。

## V3.9.7（2026-09-09）

- **存储重构：** 显示器不再拥有独立存储空间；控制台 27 格槽位改为展示**玩家手动选择的指定机箱**内存储——未选择时显示空槽与提示，点击右侧机箱按钮后绑定该机箱并实时同步物品（新增 SET_TARGET 协议，服务端切换菜单槽位绑定容器）。
- **UI 修复：** 机箱面板间距重排，输入框（阈值/数量）与方向按钮、保存按钮之间留出安全间距，消除重叠；补上缺失的「方向」标签。
- **蒙版：** 机箱存储与显示器控制台界面外侧保留与开机动画一致的灰色半透明蒙版（不再露出未变暗的游戏画面）。
- **交互：** 机箱存储/显示器控制台支持 Shift 快速移动；显示器控制台（模式 5）支持现货出售（此前被界面类型校验拦截）。

## V3.9.6（2026-09-09）

- **UI 修复：** 修复容器界面内容整体错位问题（1.20.1 在渲染说明文字前会对坐标平移，原有代码使用屏幕绝对坐标导致所有控件双重偏移；现改为面板相对坐标，三列内容与点击命中完全一致）。
- **UI 修复：** 机箱存储 / 显示器控制台的全部内容现在绘制在设备外壳的屏幕内（与开机动画共用 ComputerLayout 外壳几何，过渡无缝）：显示器外壳 560×320（屏幕 520×280 内容铺满），机箱外壳 390×460（存储窗口 196×178 居中于屏幕）。
- **UI 调整：** 机箱外壳按屏幕等比例缩放以完整显示（原框架 390×460 在小分辨率下超高溢出）；显示器与机箱均不放大、仅自适应缩小。
- **其他：** 帮助页（Explain）同类坐标错位一并修复；「启停」按钮真正生效（服务端按包内 enabled 字段切换规则）。

## V3.9.5（2026-09-09）

- 电脑机箱升级为 27 格独立存储容器（兼容漏斗/管道），点击机箱开机动画结束后打开存储界面。
- 电脑显示器升级为机箱控制台：紧邻机箱（6 方向 1 格）时动画结束后打开桌面式界面（保留现货界面边框）：左侧为与现货交易相同的界面，右侧为机箱控制面板。
- 机箱控制面板：机箱选择（位置按钮）、角色设置（购入/出售/存储）、红石模式（忽略/需信号/需无信号）、每物品自动规则（方向、阈值价格、数量、触发时机：下次价格更新/价格达到即成交/红石触发），所有选项按钮带 1px 描边；改动即时同步长期生效（NBT 持久化）。
- 自动化引擎（服务端）：资金流走机箱绑定玩家（放置者）钱包；每日价格更新后执行「下次更新」规则，每 20 刻扫描执行「达价/红石」规则（每规则每日最多一次），执行结果自动通知绑定玩家。
- 帮助页补充机箱/显示器说明；说明页文案面板内对齐修正。

## V3.9.4（2026-09-09）

- 修复帮助页标题错位：标题与正文统一使用面板内坐标。
- 合成表更新/新增：
  - 便捷交易平台：黑曜石/美元纸钞/黑羊毛 · 钻石/金锭/绿宝石 · 书架/附魔台/书架；
  - 公共市场终端：书架/青金石块/书架 · 金块×3 · 书架×3；
  - 美元纸钞（新增配方）：金块/钻石块/金块 · 钻石块/绿宝石块/钻石块 · 金块/钻石块/金块；
  - 电脑显示器（新增配方）：玻璃板/红石中继器/红石块 · 玻璃板/金块/红石块 · 玻璃板/红石比较器/红石块；
  - 电脑机箱（新增配方）：铁块/铁栅栏/铁块 · 铁块/红石块/铁块 · 铁块/铁栅栏/铁块。

## V3.9.3（2026-09-09）

- 主界面「使用说明」按钮增加 1px 描边（与交易界面按钮一致，公共按钮绘制统一），并连同余额上移至界面左上角。
- 新增可复用公共控件 `ScrollableText`（自动折行、右侧滑条、底部淡出，支持滚轮/点击/拖拽）。
- 便捷交易平台的帮助页（感叹号）改用 `ScrollableText`：文本不再超出界面范围，滑条滚动 + 底部淡出与手机说明页一致。
- 手机说明页同步改用 `ScrollableText`（行为一致，代码复用）。

## V3.9.2（2026-09-09）

- 主界面三个入口按钮（股票/期货/现货交易）增加与交易界面按钮一致的 1px 边框。
- 主界面「使用说明」按钮移至左上角，余额数据排到按钮下方。
- 手机说明页改造：文本可滚动（滑条位于右侧功能空间左侧，支持点击轨道跳转、拖拽滑块与鼠标滚轮滚动），靠近手机下边框的文字以渐变淡出。
- 现货页独立存储界面上/左/右蓝色包边去除，改为与桌面终端一致的纯格子样式（仅保留格子自身 1px 描边），两区以间隙分隔。

## V3.9.1（2026-09-09）

- 手机应用交易主界面左上角改为独立「使用说明」按钮（带悬停提示），余额恢复到按钮右侧显示。
- 点击「使用说明」按钮进入手机版说明页：展示与便捷交易平台帮助页同源的详细条目（含手机应用功能键、启动资金建议、现货存储等），点击返回或右侧返回键回到主界面。

## V3.8.10（2026-09-09）

- 便捷交易平台交易界面的帮助页（左上角感叹号）说明文本扩充：新增钱包/初始资金、启动资金获取、现货存储、稀有度、手机横屏等条目，原有条目保留。
- 手机应用交易主界面左上角改为使用说明（右侧功能键、交易入口、现货存储、启动资金建议、钱包持久化），余额数据下移排布在说明下方；股票/期货/现货交易界面保持原样。

## V3.8.9（2026-09-09）

- 玩家初始资金由 500 改为 0（可通过卖出物品赚取资金）。
- 移除「锁定式双容器柜台」方块（含方块实体、注册项、模型/贴图、配方、语言与帮助页说明）；保留便捷交易平台与公共市场终端（现货交易）。

## V3.8.8（2026-09-09）

- 新增横屏应用 API（对外接口）：`api/client/phone` 包提供 `ILandscapeApp`（「横屏」标签接口）、`ILandscapePage`（页面接口）、`LandscapePhoneChassis`（可重复使用的公共控件：旋转动画 1.2s、手机边框、右侧功能条、内容区几何）、`LandscapePhoneScreen`（通用横屏宿主屏）、`LandscapePhoneOpener`（打开入口）与 `LandscapeApps`（服务注册表，支持 `META-INF/services` 自动发现）。
- 手机应用增加「横屏」标签：实现 `ILandscapeApp` 的应用即带该标签（原版 MCphone 应用不带）；带标签应用的图标右下角显示金色「横」徽标，应用商店描述中注明「支持横屏使用」；点击带标签的应用后手机旋转至横屏，并在公共底盘上打开界面。
- 手机交易界面重构为 `MarketPage`（实现 `ILandscapePage`），`PhoneLandscapeScreen` 变为薄适配层，行为与布局保持不变。

## V3.8.7（2026-09-09）

- 现货页独立存储区改为「浅色面板 + 1px 描边」，与下方背包区之间以纯间隙自然分隔，去掉原先不协调的蓝色细线。
- 金融中心主界面金币下落动画新增生成淡入：金币生成后前 24px 内透明度渐增，与底部淡出对称。

## V3.8.6（2026-09-09）

- 修正：「买入」「卖出」按钮仅横向放大到 1.5 倍（40→60px），高度保持 16px 不变，行位置恢复原样。

## V3.8.5（2026-09-09）

- 股票/期货 K 线走势图深色背景板向右扩展 5px，走势图上下信息不变。
- 底部数量输入框及按钮行上移 7px；「买入」「卖出」按钮横向扩到 1.5 倍（40→60px，高 16→24px）并右对齐排布。
- 现货页「石头/拥有」信息行上移 5px，避免与上移后的按钮行重叠。

## V3.8.4（2026-09-09）

- 手机竖屏旋转至横屏的动画时长从 1.3 秒缩短为 1.2 秒。
- 金币下落动画：金币底缘距底部黑边 24px 时开始淡出，触碰黑线时完全消失（不再伸入黑边内）。

## V3.8.3（2026-09-09）

- 手机竖屏旋转至横屏的动画时长从 1.55 秒缩短为 1.3 秒。
- 各交易界面底部数量按钮重做：高度 13→16、宽度按文案加宽（-100 / +100 为 28px，-10 / +10 为 20px 等）、按钮间距 2→3px，按钮带 1px 描边并保证文字垂直居中；数量输入框加宽到 46px，长数字不再被截断。

## V3.8.2（2026-09-09）

- 手机现货页存储栏位加大到 16px；27 格独立存储区装在浅色底衬面板内，与玩家背包/快捷栏之间以 4px 间距和分隔线明显区分。
- 主界面金币动画改为「触碰屏幕底缘黑边即开始边下落边淡出」：金币底缘接触黑边瞬间开始降低透明度，落过边框约 9px 内淡出完毕，不再被硬裁。

## V3.8.1（2026-09-09）

- 手机主界面下落的金币动画裁剪到手机屏幕内，不再掉出边框。
- 手机现货交易页右侧物品栏升级为原版容器交互：27 格独立存储（每位玩家一份，随世界存档持久化，重开手机不丢失）+ 36 格背包；支持悬停增亮、物品详情提示（稀有度 + 行情）、点击拾取/放置、Shift 快速移动，拖拽中的物品随鼠标显示。
- 底部数量与买卖按钮行整体下移 8px，为物品栏腾出空间。

## V3.7.10（2026-09-08）

- 交易界面标题栏中板块名、时间与余额文字下移 3px（与返回按钮文字同一行），不再上飘贴边。
- K 线走势图红绿柱整体左移并满铺背景板内宽：最左侧基准日点位不再显示，「今天」的红绿柱完整落在背景板内，不再截断。

## V3.7.9（2026-09-08）

- 交易界面标题栏（返回、板块名、时间、余额）整体上移紧贴手机上边框；背景板与配色线条在不移动各控件横向位置的前提下，左端延伸到手机左边框、右端延伸到右侧功能条蒙版。
- K 线走势图背景板左侧让开列表蒙版（起点 cx+121 → cx+125），红绿柱裁剪到背景板内，不再出现在板外。
- 股票/期货页「持仓与价格」信息右移（cx+106 → cx+130），不再压住左侧列表蒙版。

## V3.7.8（2026-09-08）

- 手机交易界面内容区整体左移 15px（左缘 leftPos+40 → leftPos+25），让出右侧空间：K 线图、底部数量按钮、买入/卖出按钮等不再与右侧功能条重叠。
- 交易界面左侧搜索列表（最近点击、搜索框、物品列表）新增与右侧功能条一致的 #13193D 底衬蒙版。

## V3.7.7（2026-09-08）

- 依据 MCphone 边框贴图实测（272×432、边框 13px）修正功能条几何：功能条与蒙版条整体收进边框可视内区（长 208、贴内缘），不再与右边框、上下边框重叠。
- 手机边框贴图改为最后绘制（其内部区域透明），保证边框图层始终显示在最上面。

## V3.7.6（2026-09-08）

- 右侧功能键改用 MCphone 原版 drawNavBar 控件整体按比例放大绘制（原版 120×14 放大到内屏短边全长 220，描边随比例同步加粗），不再自绘三角/圆形/方形图标。
- 功能条与 #13193D 蒙版条整体收进屏幕边框内侧，不再压住右侧边框。
- 悬停提亮改用屏幕空间矩形实现，与放大后的三个按键区域精确对齐；点击判定复用同一组矩形。

## V3.7.5（2026-09-08）

- 右侧功能键图标线条加粗到 8px（方形/圆形/三角形描边），保持贴边蒙版与悬停提亮。

## V3.7.4（2026-09-08）

- 右侧功能条右移贴住屏幕边框，并补全上下方向的 #13193D 蒙版使三段相连；增加悬停时按键与蒙版提亮反馈。

## V3.7.3（2026-09-08）

- 右侧功能键改用 MCphone 原生 drawNavBar 绘制（含 #13193D 蒙版、原版控件与 6px 线条），并放置在不与边框重叠的侧边位置；点击判定同样使用原生 hitTestNavBar。

## V3.7.2（2026-09-08）

- 右侧功能键蒙版条紧贴手机边框，三个按键图形统一用灰色描边绘制，保证显示。

## V3.7.1（2026-09-08）

- 右侧功能键改为一条连续蒙版条，三等分三个按键区域；图标改按原生尺寸缩放渲染，确保显示。

## V3.6.10（2026-09-08）

- 右侧功能键顺序调整为方形最上、圆形居中、三角形最下；按键使用截图图标（透明处理）替换自绘图形，并添加 #13193D 背景蒙版。

## V3.6.9（2026-09-08）

- 手机旋转时长改为 1.55 秒。
- 右侧三个功能键改为清晰的描边图标（倒三角/圆形/方形），并按要求绑定：三角与 Esc=返回上一层不关手机；圆=回到 MCphone 手机桌面；方块=待定。

## V3.6.8（2026-09-08）

- 依据阳光色采样反推，调整屏外遮罩的透明度与颜色，使半透明板效果与 MCphone 一致（白色太阳透过后约为 #444444）。

## V3.6.7（2026-09-08）

- 手机屏外遮罩略微提高透明度，画面不再偏暗偏灰。

## V3.6.6（2026-09-08）

- 横屏手机屏幕内背景统一为纯色 #0C1445。

## V3.6.5（2026-09-08）

- 屏幕内改为 MCphone 同款的深藏蓝渐变，屏外改为接近原版的深灰背景。
- 功能键移到横屏手机右侧正确位置并绘制导航条，返回/主屏/任务点击区域重新校准。

## V3.6.4（2026-09-08）

- 手机屏幕内改为与 MCphone 一致的蓝紫渐变，屏外半透明板调亮并提高透明度。
- 右侧功能键按 MCphone 原始比例（40×14）绘制并旋转到侧边，返回/主屏/任务点击区域同步。

## V3.6.3（2026-09-08）

- 手机屏幕背景板缩小到边框内部，四个圆角不再外露；外侧半透明板调浅并提高透明度。

## V3.6.2（2026-09-08）

- 手机底部三键改为在横屏右侧按原图标旋转绘制，并绑定返回/主屏/任务行为。

## V3.6.1（2026-09-08）

- 手机横屏边框改由 MCphone 自带的绘制方法渲染，避免贴图拉伸出现双框/缺角；底部功能键也用原版绘制并接入正确的返回/主屏行为。

## V3.5.10（2026-09-06）

- 股票/期货 K 线图整体加大，红绿柱加粗、间距减小，左侧紧贴第 8 个最近点击物品；改用原生尺寸贴图方式绘制手机边框与功能键。

## V3.5.9（2026-09-06）

- K 线图恢复原高度并略微右移，不再下移；最近点击物品图标进一步缩小，可在同一行容纳最多 8 个。

## V3.5.8（2026-09-06）

- 修复手机边框只显示左上角的问题：改用整张贴图等比拉伸。
- 背景板随手机一起旋转；最近物品栏去掉格子背景并与搜索框/列表对齐；K 线避开最近物品栏。
- 世界金融中心标题增加缩放与左右摆动动画；±100/±10 按钮加宽避免文字超出背景。

## V3.5.7（2026-09-06）

- 重新校准手机 UI 与原始边框屏幕区域；旋转过程加入背景板。
- 现货背包格子改为与原版背包一致的主背包+快捷栏顺序，支持点击选中，尺寸加大。
- 股票/期货持仓信息与数字按钮错开排布，按钮间距加大；各板块标题增加专属配色背景框；物品列表添加缩略图。

## V3.5.6（2026-09-06）

- 手机交易板块左侧物品列表扩充到 8 行；搜索框上方新增最近点击的 8 个物品缩略图，悬停可查看详情与当日涨跌。

## V3.5.5（2026-09-06）

- 修正手机旋转方向（改为逆时针）、增加旋转背景板、按比例缩小手机边框使其完整显示。
- 手机内物品列表增加可滚动行数；现货板块右侧显示玩家 36 格物品栏。
- 金币动画改为随机生成、随机位置下落，并把金币绘制得更圆润。

## V3.5.4（2026-09-06）

- 修复手机应用商店重复出现两个方块金融图标；裁剪应用图标透明边，主界面图标更大更清晰。
- 横屏手机改用 MCphone 原始边框与底部功能图标旋转等比例放大。
- 手机内交易板块新增物品悬停详情、稀有度等级和 K 线逐日涨跌悬停提示，并恢复金币掉落动画。

## V3.5.3（2026-09-06）

- 将股票、期货、现货三个交易板块全部适配进横屏手机内屏：搜索、物品列表、K 线、数量控制和买卖均在手机内完成。

## V3.5.2（2026-09-06）

- 将方块金融手机应用进入横屏的旋转动画时间加长。

## V3.5.1（2026-09-06）

- 接入 MCphone 应用 API：方块金融以独立应用形式出现在手机应用商店，可安装到主界面并排在现有应用之后。
- 点击已安装的方块金融应用后进入自建横屏手机界面：带 90° 慢速旋转动画、侧面功能键与适配手机边框的交易主界面。

## V3.4.2（2026-09-06）

- 删除「高清手机」物品及其模型、贴图、代码和语言条目，保留电脑显示器与机箱。

## V3.4.1（2026-09-06）

- 移除本模组独立创造标签页，全部物品改挂到前置模组 MCphone 的标签页中。

## V3.3.10（2026-09-06）

- 为模组增加 Forge 标准的 logoFile 与根目录 logo.png，供 PCL 等启动器的模组管理页显示缩略图。

## V3.3.9（2026-09-06）

- Phone 内容正式合并为单一模组：移除独立子模组声明，手机/显示器/机箱全部进入「方块金融」创造标签页。

## V3.3.8（2026-09-06）

- 原封不动合并 Phone 整包内容（mod_3ce985ee / 高清手机），版本号按用户要求向后迭代 29 个版本。

## V2.6.9（2026-09-06）

- 将模组列表中致谢语改为更具体、正式的书面表达。

## V2.6.8（2026-09-06）

- 精简游戏模组列表中的介绍信息，并新增对 MCphone 作者 november521 的特别感谢。

## V2.6.7（2026-09-06）

- 声明 MCphone 0.10.2+ 为硬性前置模组，缺失时 Forge 会在启动阶段报错并提示需要 mcphone。

## V2.6.6（2026-09-06）

- 帮助页补充行情系统说明：股票/现货共用一套走势，期货独立；大跌幅度区间说明。

## V2.6.5（2026-09-06）

- 使用本机 Pillow 去除同花顺封面图的白色背景，并将透明版 LOGO 应用到模组标签页。

## V2.6.4（2026-09-06）

- 更换模组标签页 LOGO 为重新制作的同花顺 APP 像素图。

## V2.6.3（2026-09-06）

- 新增「方块金融」专属创造模式标签页，标题使用模组名，标签图标采用提供的同花顺风格封面图；本模组物品全部移入该标签页。

## V2.6.2（2026-09-05）

- 按更正细化非上限涨停/跌停后的判定：上涨中涨停后 40% 维持涨停（最多 3 日）、30% 继续上涨、30% 转下跌；下跌中跌停后 40% 维持跌停（最多 3 日）、30% 继续下跌、30% 转上涨。

## V2.6.1（2026-09-05）

- 修正未达连涨/连跌上限时触发的涨停/跌停逻辑：之后每天重新判定继续涨停、继续跌停或反向波动（各 1/3 概率），不再固定连续限价。

## V2.6.0（2026-09-05）

- 涨停/跌停改为持续 1-3 天的事件机制；连续上涨 6 天或连续下跌 4 天后，次日以 35%-70% 随机概率继续涨停/跌停而非强制反向。
- 未到连涨/连跌上限但即将反向时，有 15%-45% 随机概率转成涨停/跌停。
- 股票与期货拆分为独立价格序列：股票大跌幅度 0.6-0.95 倍，期货大跌幅度 0.5-1.5 倍。

## V2.5.0（2026-09-05）

- 股票/期货的「已拥有」按钮改为填入所选物品的实际持仓数量，未选择时默认使用第一个物品。
- K 线规则调整：连续下跌不超过 4 天，连续上涨不超过 6 天。
- 稀有度档位更名为：普通、稀有、史诗、传说；悬停物品时在名称下方用不同颜色显示稀有度。
- 初始定价改为按档位生成：普通 30-100、稀有 101-1000、史诗 1001-10000、传说 10001-100000。
- 连续上涨 2 天后开始有概率触发次日大跌：第 3 天 5%，逐日上升，第 6 天达到 55%；跌幅为该档最大跌幅的 0.6-0.95 倍。

## V2.4.0（2026-09-05）

- 修复每天首次打开交易平台时物品按钮仍显示昨日价格的问题：收到新报价即刷新列表文字。
- 行情加入均值回归：连续上涨/下跌 3 天后开始向反方向回拉，最长连涨或连跌不超过 5 天。
- 每日波动幅度按物品稀有度在限幅内动态浮动。

## V2.3.1（2026-09-05）

- 修复时间回拨后行情不再随后续天数推进的问题：回拨时重置各物品的更新基准日，价格不回滚，之后前进可继续刷新走势。
- 主界面比特币数量增加到 10 个。

## V2.3.0（2026-09-05）

- 数量增减按钮改为 -100、-10、-1、+1、+10、+100 的左右对称顺序排列。
- 睡觉或用指令跳过时间时，各交易板块会重新拉取价格与 15 日走势。
- 物品连续涨停或跌停最多持续 2 个游戏日，第 3 日必然打破限价。
- 主界面比特币改为从顶部随机位置生成，以半速完整下落到底部后重新随机生成并再次下落。

## V2.2.3（2026-09-05）

- 股票/期货/现货界面在游戏日变化时会批量刷新当前列表中的行情与 15 日走势，用指令快进游戏时间后价格和 K 线会跟随更新。

## V2.2.2（2026-09-05）

- 股票/期货近 15 日走势仅在鼠标靠近 K 线柱时弹出详情。
- 走势详情日期改为倒序表示：今天、前1天、前2天……前14天。
- 主界面比特币动画改为左右均匀分布下落。

## V2.2.1（2026-09-05）

- 稀有度档位名称：少见改为罕见；史诗档每日最大涨跌幅调整为 ±100%。
- 各档位每日波动幅度：普通 ±10%、罕见 ±22%、稀有 ±45%、史诗 ±100%。

## V2.2.0（2026-09-05）

- 主界面比特币数量减半、下落速度放慢；底部 K 线改为更夸张的长影线、大实体和发光效果。
- 修复股票/期货 K 线悬停不显示当日价格与较前日涨跌幅的问题（手动补上 tooltip 调用）。
- 行情波动改为按物品稀有度分级：越稀有波动越大，且不再设置统一的涨跌幅上限。

## V2.1.2（2026-09-04）

- 主界面比特币动画避开按钮区域，不再遮挡按钮视线。
- 平台现货交易按 Esc 返回世界金融中心主界面。
- 分红与各交易提示中的物品名称改为中文显示，悬停物品信息时额外显示物品 ID。

## V2.1.1（2026-09-04）

- 各交易板块的涨跌颜色改为国内市场的「红涨绿跌」：K 线、涨跌文字和悬停提示统一使用红色表示上涨、绿色表示下跌。

## V2.1.0（2026-09-04）

- 主界面左上角新增深色方框的浅色感叹号帮助按钮，点击打开模组操作与功能介绍页，悬停显示提示。

## V2.0.0（2026-09-04）

- 主界面比特币动画改为圆形金币样式，并固定绘制在文字、按钮和图层的下方。
- 主界面 K 线装饰移到按钮下方的独立行情带，不再被按钮遮挡。
- 股票/期货 15 日走势升级为红绿涨跌柱与每日点位标记，鼠标悬停某一天时显示该日价格和较前日的涨跌百分比。

## V1.1.0（2026-09-04）

- 行情刷新改为严格按游戏刻驱动，使用指令修改世界时间时，界面倒计时和行情更新时间会立即重新计算。
- 移除平台充值入口及相关功能，主界面改为华尔街风格深色背景、动态金色标题和比特币掉落动画，三个入口按钮并排显示。
- 股票实现 T+1（当日买入次日可卖）、买卖佣金与印花税、每日分红。
- 期货实现双向开仓（做多/做空）、10% 保证金、每日盯市结算、3 个游戏日到期交割、穿仓强制平仓与固定手续费。
- 放大股票/期货界面，修正数量输入框旁按钮文字和数字显示不全的问题。

## V1.0.1（2026-09-04）

- 现货界面退出重进后保留上次选中的物品及行情显示。
- 独立存储区支持原版容器交互：Shift 移动、数字键切换、双击收集、拖拽分配。
- 现货卖出改为在独立存储区、玩家背包和物品栏中共同查找并扣除目标物品。
- 「最大」按钮改为目标物品的单格最大堆叠数量。
- 各交易板块新增「已拥有」按钮，填入玩家背包/物品栏/存储区内已拥有的目标物品数量。

## V1.0.0（2026-09-04）

- 发布版本命名采用 `_V1.0.0` 后缀；由于 Forge 强制要求 mods.toml 的 version 为纯数字，内部版本号为 `1.0.0`。
- 所有交易界面右上角显示游戏时间与距行情更新的剩余时间。
- 现货买入改为直接扣款发物品到背包；现货卖出从界面右侧的额外存储格扣除。
- 便捷交易平台现货界面：9 物品栏 + 27 背包 + 27 平台存储；公共市场终端：9 物品栏 + 27 背包 + 36 终端存储。
- 移除存储区初始种子物品，并为每个格子绘制边框线。
- 平台现货界面增加返回按钮。
- 股票/期货/现货搜索支持中文与英文物品 ID 检索。
- 股票/期货/现货/充值增加数量输入框、±1/±10/±100、最大/最小按钮。

## 1.0.0-r1（2026-09-03/04）

- 初版：钱包、股票/期货行情、15 日走势、公共市场终端、双容器柜台。
- 作者字段改为 Xinde。
